project('app').ext {
    // 构建相关配置
    androidBuild = [
            myCompileSdkVersion: 28,
            myBuildToolsVersion: '28.0.3',
            myMinSdkVersion    : 17,
            myTargetSdkVersion : 28
    ]
    // 应用相关配置
    appConfig = [
            myVersionCode: 1,
            myVersionName: '1.0.0'
    ]
}
