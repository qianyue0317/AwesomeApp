进程保活 通过监听屏幕状态

jni java kotlin 混合开发
启动优化

生成设备唯一的uuid

如果接口需要本次数据库存储 需要将数据层但分出来, 不直接在presenter中写

如果一个代码块(方法内)需要多次调用一个方法的返回值(并且返回值跟调用次数无关),好的习惯是
用一个变量保存第一次调用该方法的返回值,后边直接用该变量访问

LinkedList 的优势在于"随机"  如果是直接在后边插入 和ArrayList相比较 在规模大的时候ArrayList更优秀

此项目的数据持久化决定采用LitePal不使用GreenDao

抽象事务管理

剪贴板

Javassist

即时通讯解决方案 https://github.com/wildfirechat/android-chat.git

写一个朋友圈九宫格的图片展示控件

livedatabus

mvvm

视频播放 JiaoZiVideoPlayer

位运算可以减少不必要的if